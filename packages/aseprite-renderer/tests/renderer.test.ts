import { test, expect } from "bun:test";
import { AsepriteRenderer } from "../src/asepriteRenderer";

test("renderer should be defined", () => {
    expect(AsepriteRenderer).toBeDefined();
});

test("placeholder test - add more tests here", () => {
    expect(true).toBe(true);
});
